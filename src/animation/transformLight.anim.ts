import Animation from '@/animation/animationObj'
import TAnimationProperties from '@/types/other/animation.types'

const transformLightAnim = new Animation({
    initialStyles: {},
    animationStyles: {},
} as TAnimationProperties)
export default transformLightAnim
