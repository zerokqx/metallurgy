import styled from "styled-components";

export const FooterStyled = styled.footer`
    padding: 10px;
    height: 70px;
    background: ${({theme})=> theme.background.white};
    border-top: ${({theme})=> theme.border.borderPlatinum} 1px solid;
    width: 100%;
    display: flex;
    
    

`